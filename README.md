This Project is a type of encoder and decoder file through a password.
- The encoding works as follows:
1. Reads the .txt file and transform each letter into a correspondant image
2. Then will shift the images in a certain pattern
- The password should update the output to the encoded message.
